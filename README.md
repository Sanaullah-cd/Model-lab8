# 🧩 Модуль 08 — Структурные паттерны: Декоратор и Адаптер

## 🎯 Цель
Изучить применение структурных паттернов **Декоратор** и **Адаптер** в C# для гибкого расширения функциональности объектов и интеграции разных интерфейсов.

---

## ☕ Паттерн «Декоратор»
Позволяет динамически добавлять новую функциональность объекту без изменения его структуры.

### 🔹 Пример
```csharp
IBeverage drink = new Coffee();
drink = new MilkDecorator(drink);
drink = new SugarDecorator(drink);
drink = new ChocolateDecorator(drink);
drink = new VanillaDecorator(drink);
Console.WriteLine(drink.GetDescription());
Console.WriteLine(drink.GetCost());
